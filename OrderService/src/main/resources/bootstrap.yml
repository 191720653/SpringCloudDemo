spring:
  cloud:
    config:
      name: order-service
#      label: master
      discovery:
        enabled: true
        service-id: ConfigCenter
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8081/eureka/